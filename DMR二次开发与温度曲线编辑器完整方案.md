# DMR 二次开发与温度曲线编辑器完整方案

> 文档版本：1.1  
> 编写日期：2026-07-18；更新日期：2026-07-19  
> 当前实现名称：**DMR Curve Studio**  
> 目标：不破坏 DMR 原程序，通过读写其原生 `.PLR` 历史文件，实现温度曲线的可视化、逐点编辑、批量生成、字节级校验、DMR 重新打开以及客户图片导出。

---

## 1. 最终结论

这件事可以实现，而且不需要直接修改 DMR 的 `DMEngine.exe`。

最适合当前项目的路线不是给闭源 EXE 打补丁，而是做一个 **DMR 外挂式曲线编辑器**：

1. 读取 DMR 原始 `.PLR`；
2. 读取 DMR 从同一份 `.PLR` 导出的 `.xls`；
3. 自动把 Excel 的时间/温度序列与 PLR 的环形物理记录对齐；
4. 在图形界面中逐点拖动或批量生成目标温度；
5. 只改 PLR 数据区内指定通道、指定记录的两个字节；
6. 保留原文件长度、头部、索引、设备配置和所有非目标字节；
7. 输出新 `.PLR`，仍由原版 DMR 打开；
8. 同时直接导出客户所需的高清 PNG 和修改清单。

这相当于对 DMR 做“旁路二次开发”：DMR 仍负责识别、显示和导出，DMR Curve Studio 补上 DMR 缺少的“编辑并保存曲线”能力。

---

## 2. 已安装 DMR 软件的分析结果

### 2.1 软件版本和安装位置

本机实际安装信息：

| 项目 | 结果 |
|---|---|
| 软件名 | DMR |
| 版本 | 3.20.0 |
| 主程序 | `C:\Program Files (x86)\DMR\dmr\DMEngine.exe` |
| 主程序位数 | PE32 / x86 |
| GUI 技术 | 原生 Windows / MFC 运行库 |
| `.PLR` 文件关联 | `RDZFile` |
| 打开命令 | `C:\PROGRA~2\DMR\dmr\DMEngine.exe %1` |

主程序中可以看到 `CCurveCtrl`、`CHisCurveDlg`、`CExcelDlg`、`CSaveAsDlg`、`CDataList` 等类名，说明 DMR 内部已经有曲线显示、历史数据、Excel 导出和另存为模块，但没有暴露可编辑后回写历史数据的功能。

### 2.2 DMR 的分析器插件结构

安装目录包含：

```text
C:\Program Files (x86)\DMR\dmr\Analyzer\
  ArfAnalyzer.dll
  DatAnalyzer.dll
  GrfAnalyzer.dll
  MCAnalyzer.dll
```

四个 DLL 都是 32 位 PE，每个 DLL 暴露相同的 4 个导出函数：

```text
GetDevice
GetDocString
GetFilter
GetRegisterInfo
```

`MCAnalyzer.dll` 中还存在 `MCAnalyzer`、`GetAnalyzer` 等符号。由此可以判断 DMR 采用“主程序 + 文件分析器 DLL”的结构，根据文件类型加载分析器。

### 2.3 三种“二次开发”路线比较

| 路线 | 做法 | 优点 | 缺点 | 结论 |
|---|---|---|---|---|
| A. 修改 `DMEngine.exe` | 反汇编、Hook 曲线控件、增加保存入口 | 看起来像原生功能 | 版本强耦合、崩溃风险高、每次升级重做、测试困难 | 不作为主路线 |
| B. 编写 Analyzer DLL | 模拟 `MCAnalyzer.dll` 接口，注册新格式 | 能嵌入 DMR 的文件加载流程 | 缺少 C++ 接口头文件和 ABI；仍不一定能获得编辑 UI | 作为长期研究路线 |
| C. 外挂式兼容编辑器 | 独立编辑 `.PLR`，再由 DMR 打开 | 稳定、可测试、不改安装目录、可做完整 GUI | 需要掌握 PLR 格式 | **当前主路线** |

本项目已有探针结果证明 `.PLR` 没有整体加密，修改温度字段后 DMR 可以继续打开和导出。因此路线 C 的成功条件已经满足。

---

## 3. VX5300 `.PLR` 格式的当前结论

### 3.1 当前样本

```text
设备/文件签名：MCA63R1041SX400
文件大小：76238 字节
DMR 原始 Excel 有效数据：2329 行
Excel 有效时间：2023-11-06 10:00:00 ～ 2023-11-09 15:36:00
采样间隔：2 分钟
```

### 3.2 文件结构

```text
0 ～ 4213                    头部、设备、通道配置、环形缓冲状态/索引
4214 ～ 76237                3001 条物理记录

每条记录：24 字节
每条记录：12 个 little-endian int16 字段
数据区大小：3001 × 24 = 72024 字节
头部大小：76238 - 72024 = 4214 字节
```

物理地址公式：

```text
record_offset = 4214 + physical_record_index × 24
field_offset  = record_offset + field_index × 2
```

### 3.3 字段和通道

当前样本中可编辑的温度字段：

```text
field_01 -> 通道01
field_02 -> 通道02
...
field_09 -> 通道09
```

`field_00` 不是普通温度通道。`field_10`、`field_11` 以及 Excel 通道10～12在当前样本中主要表现为 0、断线值或无效值，当前24字节样本的安全默认配置不写入这些字段。

旧项目的探针实验已经用 DMR 导出确认 `field_01`、`field_02`、`field_03` 分别只影响对应通道；本次又使用通道1～9的 Excel/PLR 轨迹进行了联合相关匹配。

通道数量在程序结构中不再固定为9。对于16通道及以上设备，可在高级格式参数中同时填写实际记录字节和温度通道数；例如连续 `field_01～field_16` 至少需要34字节记录。解析器会校验最高通道字段是否落在单条记录边界内，超界时直接拒绝载入。此扩展能力不改变当前24字节样本只开放已验证 `field_01～field_09` 的安全默认值。

### 3.4 温度变化与 raw 的关系

已验证的增量关系：

```text
delta_raw = round(delta_temperature × 48)
```

即：

```text
温度提高 1℃   -> raw 增加约 48
温度提高 10℃  -> raw 增加约 480
温度降低 5℃   -> raw 减少约 240
```

正式编辑器采用“在原 raw 上加温差”的方式：

```text
raw_new = raw_old + round((target_temp - original_excel_temp) × 48)
```

而不是简单使用 `raw_new = target_temp × 48`。这样可以保留每个采样点原有的小数残差和传感器轨迹特征，也与已经通过 DMR 导出验证的旧脚本算法一致。

### 3.5 环形缓冲区和时间映射

PLR 中的 3001 条是**物理环形记录**；Excel 中的行是按时间排序的**逻辑记录**。两者不能直接认为“Excel 第 i 行就是 PLR 第 i 条”。

本次对真实样本使用 1～9 通道、180 个分布采样点进行全缓冲区搜索，结果为：

```text
Excel 第一条 2023-11-06 10:00:00 -> PLR physical_record_index = 530
平均绝对匹配误差约 0.821℃
次优物理起点 = 529
次优平均误差约 1.260℃
```

因此：

```text
physical_record_index = (530 + excel_row_index) mod 3001
```

例如：

```text
2023-11-06 15:42:00 是 Excel 0 基第 171 行
physical_record_index = 530 + 171 = 701
```

### 3.6 对旧脚本的重要修正

旧 JSON 配置写的是：

```json
"anchor_time": "2023-11-06 15:42:00",
"anchor_record_index": 700
```

但真实 DMR 导出对照显示：

```text
physical 700 实际对应 2023-11-06 15:40:00
physical 701 才对应 2023-11-06 15:42:00
```

旧脚本因此存在一个采样点（2 分钟）的偏移。DMR Curve Studio 不再依赖人工硬编码的 anchor，而是自动搜索物理起点；同时保留“手工指定 Excel 首行物理记录”的高级入口。

---

## 4. 现有 Python 项目审计

### 4.1 已有能力

现有 `plr_temperature_editor` 已实现：

- 读取固定格式 PLR；
- 读取 DMR `.xls`；
- `offset` 整体偏移；
- `absolute_clamp` 绝对限幅；
- `trend_clamp` 趋势限幅；
- `window_delta_clamp` 单位时间温差限制；
- 手动保留时间段；
- 自动识别停火骤降；
- CSV / JSON 修改报告；
- DMR 再导出 Excel 后的对比程序；
- 规则层单元测试。

这些脚本证明了 PLR 可写、通道字段可控和温差/raw 比例，是非常重要的前期验证。

### 4.2 不能满足最终目标的原因

1. 必须手工写 JSON，客户需求变化时操作成本高；
2. 不能直接看见和拖动目标曲线；
3. 不能逐点输入目标温度；
4. 时间到物理记录依赖硬编码 anchor，而且当前样本有一点偏移；
5. 没有撤销/重做；
6. 没有一个独立于 DMR 的客户图片输出流程；
7. 没有在导出前做“所有非目标字节不变”的强校验；
8. 当前电脑没有可直接调用的 Python 运行时，而 Node.js 已安装；
9. 固定配置不利于以后增加其他型号/记录长度的格式档案。

### 4.3 改造策略

保留 Python 代码和探针文件作为逆向证据及回归参考；新增一套 Node.js + 浏览器 GUI。核心算法不依赖服务器处理用户文件，PLR 和 Excel 都在浏览器本地内存中解析，Node.js 只负责提供本地页面。

---

## 5. DMR Curve Studio 的产品方案

### 5.1 用户流程

```text
载入原始 PLR + 原始 Excel
        ↓
自动识别文件结构、通道、采样间隔
        ↓
多通道轨迹相关匹配，确定环形缓冲物理起点
        ↓
选择通道和时间范围
        ↓
拖动曲线 / 逐点输入 / 批量生成
        ↓
撤销、重做、对比原曲线
        ↓
字节级兼容性校验
        ↓
输出 edited.PLR + PNG + CSV + project.json
        ↓
用 DMR 打开 edited.PLR 做最终显示/导出
```

### 5.2 当前 GUI 功能

#### 文件载入

- 拖放或选择 `.PLR`；
- 拖放或选择同源 `.xls/.xlsx`；
- 一键载入项目内真实示例；
- 自动反推数据区偏移；
- 可手工设置 `dataOffset`、`recordSize`、`totalRecords`、`rawPerCelsius`。

#### 自动映射

- 自动扫描 3001 个可能的物理起点；
- 使用多个通道和均匀分布的采样点评分；
- 显示最佳起点、平均误差、置信度；
- 允许手工重新映射。

#### 可视化编辑

- 原始曲线灰色、编辑结果橙色；
- 鼠标按住拖动，连续绘制目标曲线；
- 双范围滑块选择编辑时间段；
- 图表显示全部或聚焦选中范围；
- 参考 DMR 提供 `1h / 4h / 8h / 12h / 24h` 时间轴档位；
- 鼠标滚轮以指针为中心缩放，`Shift + 滚轮` 前后平移；
- 可切换“平移查看”后拖动画布，双击曲线复位；
- 图表显示十字光标、当前时间点和目标温度；
- 逐点表格直接输入目标温度；
- 30 级撤销/重做。

#### 多通道叠加与编辑模型

界面把“展示通道”和“编辑通道”明确分成两个概念：

- **展示通道**：决定图表上叠加哪些曲线，可选“仅当前”“全部”，也可在动态通道复选网格中自由组合；
- **编辑通道**：任何时刻只有一个，可由下拉框或点击图表图例切换；
- 当前编辑通道始终处于展示状态，不能被单独隐藏；
- 各通道使用固定颜色，切换编辑通道不会改变通道颜色；
- 当前编辑通道目标线加粗，并额外显示灰色虚线原始值；
- 其他展示通道只显示目标线，降低大量通道同时显示时的视觉噪声；
- 悬浮提示同时列出全部已展示通道温度，并标明当前编辑通道、原始值和已修改状态；
- 1～24通道使用固定且互不重复的颜色，更多通道使用确定性的扩展色相，切换和导出时颜色保持一致；
- 悬浮提示在通道较多时自动分栏，避免超出图表或遮住正在查看的曲线点；
- 多通道 PNG 使用同一套颜色和图例，图例按图片宽度自动换行，并同步增加画布高度，始终与曲线绘图区分离。

温度编辑采用“多通道对比、单通道落笔”的安全方式：

1. 多条曲线可同时叠加对比；
2. 先选定唯一的编辑通道；
3. 拖动绘制、逐点表格、恢复原值和所有批量算法只作用于该编辑通道；
4. 切换图例到另一通道后，再对该通道进行下一轮修改；
5. 顶部标题、批量按钮和数据表会同步显示当前编辑通道，避免误操作。

没有把“所有展示通道”直接等同于“批量修改通道”，因为客户要求、传感器偏差和 PLR raw 字段均按通道独立，直接群改风险较高。当前方案兼顾叠加分析效率与写入安全性。

当确实需要处理燃烧机人为操作造成的多测点共同异常时，可显式把“应用通道范围”切换为“全部已展示通道（共同调整）”。这是一个有明确提示、可整体撤销的协同编辑入口，不会因为普通的多通道展示而自动群改。

#### 多通道协同燃烧修复

多通道协同模式不是把一条曲线简单复制到其他通道，而是构建“共同炉况 + 测点差异”的分层模型：

```text
通道目标温度
  = 通道自身温度基准
  + 同步后的共同慢趋势
  + 共同燃烧随机波动 × 通道响应系数
  + 少量通道独立残差
  + 共同温度调整
```

处理步骤：

1. 分别对各通道计算慢趋势和原始残差；
2. 把各通道相对自身起始温度的趋势求为共同趋势；
3. 使用一套共享 OU 随机过程、慢漂移、变周期和燃料脉冲作为共同燃烧驱动；
4. 按“共同波动比例”决定共享驱动占比；
5. 按“趋势同步比例”修复各通道慢趋势的一致性；
6. 使用小幅、可复现的“通道响应差异”，模拟测点位置、热惯性和传感器响应不同；
7. 将任一通道检测到的违规窗口合并为联合掩码，使已选通道在同一异常时段共同修复；
8. 升温阶段先对共同驱动保留约束余量，再逐通道投影到最终上限，避免各通道因独立限幅失去相关性；
9. 保温阶段逐通道保证窗口最大温差；
10. 为每个通道建立独立客户规则并统一执行导出门禁。

关键参数：

| 参数 | 建议值 | 作用 |
|---|---:|---|
| `sharedRatio` | `0.85` | 共同燃烧波动比例；越高，各通道升降节奏越一致 |
| `trendSyncRatio` | `0.80` | 慢趋势同步比例；越高，各通道大方向越一致 |
| `channelVariation` | `0.08` | 各测点对共同波动的响应差异，防止完全重合 |
| `commonOffset` | `0℃` | 所有已选通道共同升高或降低的温度 |
| `preserveRatio` | `0.45` | 保留各通道原始细节的比例 |

默认参数下，合成测试的通道增量相关系数大于 `0.90`。真实 `DAT0131` 通道01、03、05协同升温回归中，三组两两增量相关系数为 `0.9886～0.9994`，但通道温差和独立细节仍保留。

#### 图上手动协同编辑

图表工具栏提供独立的“协同绘制”开关。开启后，当前编辑通道仍作为鼠标控制基准，但每次点选和拖动会联动所有已展示通道。

单点计算：

```text
delta = 鼠标目标温度 - 当前编辑通道该点的手势开始前温度
每个已展示通道的新温度 = 该通道手势开始前温度 + delta
```

连续拖动时，在前后两个鼠标点之间对 `delta` 做线性插值，再把每个时间点的同一温差应用到所有通道。因此：

- 当前编辑通道会准确经过鼠标轨迹；
- 其他通道同步改变升降方向、波动形状和温度变化量；
- 通道之间原有的点对点温差保持不变；
- 断线或无效温度点保持无效，不会被凭空补值；
- 一次按下到松开的完整手势只产生一条撤销记录；
- 切换当前编辑通道不会关闭协同模式；
- 展示通道减少到一个时，协同模式自动关闭；
- 平移查看模式与协同绘制模式相互独立，开启协同绘制时会自动返回绘制状态；
- 手工修改后客户规则自动变为待校验，PLR/PNG 导出门禁继续生效。

这种方式适合操作者已经知道某个局部时间点或一段趋势应该提高/降低多少，希望在图上直接精修，而不是重新生成整段随机曲线的场景。

#### 批量生成模式

1. 整体升高/降低；
2. 设为恒定温度；
3. 线性升温/降温；
4. 限制温度上下界；
5. 滑动平均平滑；
6. **真实燃烧合规修复**；
7. 叠加正弦波动（兼容旧需求）；
8. 限制单位时间窗口最大温差；
9. 恢复选中范围的原始值。

#### 客户验收要求

- 可录入多条升温或保温规则；
- 每条规则包含名称、通道、阶段、开始/结束时间、窗口和限值；
- 可一键把当前选区写入规则，也可精确编辑时间；
- 升温规则校验窗口端点温升并折算为 `℃/h`；
- 保温规则校验滚动窗口内 `max - min`；
- 逐规则显示最大观测值和违规窗口数；
- 要求修改、曲线修改、撤销/重做后自动使旧校验失效；
- 默认启用导出门禁：无要求、未校验或任一规则失败时，禁止导出 PLR 和客户 PNG；
- CSV 始终允许导出，便于定位和排查违规点；
- 客户要求随项目 JSON 保存和恢复。

#### 导出

- DMR 可识别的 `.PLR` 副本；
- 高清客户曲线 `.png`，可填标题、客户/项目、说明；
- 包含时间、通道、物理记录、原始/目标温度、原始/新 raw 的 CSV；
- 可继续编辑的项目 JSON。

### 5.3 文件兼容性校验

每次生成 PLR 前必须满足：

```text
output.length == original.length
```

并逐字节检查：

```text
头部 0～dataOffset-1：必须完全相同
未选中的记录：必须完全相同
未选中的通道字段：必须完全相同
每个变化字节：必须落在编辑计划中的 int16 两字节范围
raw_new：必须在 -32768～32767
```

校验结果在界面显示：

- 文件长度；
- 计划修改点数；
- 实际变化字节数；
- 头部变化字节数；
- 非目标变化字节数。

只有所有检查都通过才下载输出 PLR。

### 5.4 真实燃烧波动与合规修复

旧式正弦叠加会产生幅值和周期过于整齐的曲线，不符合燃料供给、风量、炉膛迟滞和测量噪声共同作用下的真实观感。新模式 `realistic_combustion` 不使用单一固定周期，而是组合以下成分：

1. **OU / AR 型相关随机扰动**：相邻点有关联，不会变成逐点白噪声；
2. **慢漂移**：表现燃料热值、风量和负荷随时间缓慢变化；
3. **游走周期**：周期在一定范围内随机变化，而不是重复正弦；
4. **稀疏燃料脉冲**：按每小时事件率产生少量正负扰动并逐步衰减；
5. **原曲线残差融合**：保留传感器和真实炉况已有的细节；
6. **确定性随机种子**：相同源数据、参数、选区和种子得到完全相同的结果，便于审核和复现。

主要参数：

| 参数 | 含义 |
|---|---|
| `phase` | `heating` 升温或 `holding` 保温 |
| `windowMinutes` | 客户规则滚动窗口，默认 60 分钟 |
| `maxRisePerHour` | 升温阶段每小时最大正温升，默认 5℃/h |
| `maxFluctuation` | 保温窗口最大温差，默认 5℃ |
| `amplitude` | 自然波动强度 |
| `preserveRatio` | 原曲线残差保留比例 |
| `correlationMinutes` | 随机波动相关时间 |
| `eventsPerHour` | 燃料脉冲平均发生次数 |
| `transitionMinutes` | 选区边界过渡宽度 |
| `seed` | 可复现随机种子 |
| `onlyViolations` | 只处理违规窗口和扩展过渡区 |

生成完成后还会执行约束投影：

```text
升温：任意完整滚动窗口的正温升 / 窗口小时数 <= maxRisePerHour
保温：任意滚动窗口的 max(target) - min(target) <= maxFluctuation
```

升温投影会保留随机余量，避免连续受限点退化成固定周期或完全笔直的斜线；保温投影按历史窗口动态限制上下界。算法只返回选区内的修改，选区外数组逐点保持不变。启用 `onlyViolations` 后，远离违规窗口的合规区保持原值。

### 5.5 DMR 时间轴交互参考

DMR 原界面采用小时档位、左右追忆和倍率缩放的思路。Curve Studio 对应实现为：

- `1h / 4h / 8h / 12h / 24h / 全部` 快速档位；
- 左右按钮按当前窗口约 70% 前后移动；
- 放大/缩小按钮和滚轮连续缩放，可覆盖 DMR `1 / 2 / 4 / 8 / 16 / 32` 倍的使用场景；
- `Shift + 滚轮` 或“平移查看”拖动进行时间追忆；
- 双击或“复位”返回全部记录。

---

## 6. 技术架构

```text
public/index.html            可视化编辑界面
public/styles.css            UI 样式
public/app.js                交互、Canvas 曲线、撤销/重做、导出
public/vendor/xlsx...js      浏览器本地解析 BIFF .xls
src/plr/core.js              PLR/Excel/对齐/编辑/校验核心
src/server.js                仅监听 127.0.0.1 的静态本地服务
tests-js/core.test.js        自动化回归测试
fixtures/                    真实 DAT0131 回归样本
启动DMR曲线编辑器.cmd         Windows 一键启动
```

### 6.1 核心数据模型

```text
session
  plr.data                   原始 PLR 字节副本
  plr.layout                 数据偏移、记录长度、记录数
  excel.rows                 时间有序的逻辑温度记录
  alignment.startRecordIndex Excel 首行对应的物理记录
  rows[i].physicalRecordIndex
  channels[ch].original[i]   DMR Excel 原始温度
  channels[ch].targets[i]    用户编辑后的目标温度
  channels[ch].raw[i]        PLR 原始 int16
```

### 6.2 为什么需要同时导入 PLR 和 Excel

PLR 存的是设备内部 raw 和环形记录，Excel 提供 DMR 最终解释后的时间、显示温度和无效值。两者一起使用有四个好处：

1. 不需要完整复制 DMR 内部每一种热电偶/量程换算；
2. 可以知道客户看到的原始温度；
3. 可以自动找到环形缓冲物理位置；
4. 修改时只需计算“目标温度与原显示温度的差”。

以后若完全解析出 PLR 头部中的类型、量程、时间和环形指针，可以增加“仅导入 PLR”模式，但当前双文件模式可靠性更高。

---

## 7. 测试和真实样本证据

当前 Node.js 自动化测试覆盖：

1. 旧式 BIFF `.xls` 表头和无效温度解析；
2. 跨环形尾部的自动物理记录对齐；
3. 按真实温差写 raw；
4. 非目标字节和头部变化拦截；
5. 偏移、线性、限幅、平滑、窗口温差工具；
6. 真实燃烧模式相同种子复现、不同种子差异和非固定周期；
7. 多通道共同波动相关性、联合违规掩码和缺失通道前缀；
8. 16通道扩展记录读写和字段边界拦截；
9. 图上协同绘制的同温差插值和通道差保持；
10. 升温 `<= 5℃/h` 与保温窗口温差 `<= 5℃`；
11. 局部选区隔离和仅修复违规区；
12. 客户要求违规发现、修复后通过和项目 JSON 往返；
13. 真实 `DAT0131.PLR + curve.xls` 回归；
14. DMR 3.20.0 单点、线性、平滑、单通道真实燃烧、多通道协同燃烧五组回读 Excel；
15. 本地服务器健康检查、核心脚本和示例文件路由。

真实样本自动测试确认：

```text
自动物理起点 = 530
2023-11-06 15:42:00 -> physical 701
通道01原显示温度 = 89℃
设置目标 100℃ -> delta_raw = 11 × 48 = 528
输出长度 = 76238
头部变化 = 0
非目标变化 = 0
```

运行命令：

```powershell
npm.cmd test
npm.cmd run check
```

---

## 8. 开发阶段与验收标准

### 阶段 1：文件格式和算法核心（已完成）

- [x] 解析真实 PLR；
- [x] 解析 DMR BIFF `.xls`；
- [x] 自动物理记录对齐；
- [x] 多通道温度数据模型；
- [x] 修改计划和 raw 写入；
- [x] 字节级兼容性校验；
- [x] 真实样本回归测试。

### 阶段 2：可视化编辑器（已完成首版）

- [x] 文件拖放；
- [x] 曲线预览；
- [x] 鼠标拖动编辑；
- [x] 逐点表格编辑；
- [x] 时间范围选择；
- [x] 九种批量/恢复工具；
- [x] 真实燃烧非规律波动与升温/保温约束；
- [x] 客户要求录入、校验和导出门禁；
- [x] DMR 时间轴档位、滚轮缩放和平移追忆；
- [x] 撤销/重做；
- [x] PLR / PNG / CSV / JSON 导出。

### 阶段 3：DMR 端到端验收（已完成）

每一个正式版本至少用 DMR 3.20.0 完成：

1. 用 DMR 打开原始 PLR 并导出原始 Excel；
2. 在 Curve Studio 修改三个已知时间点；
3. 生成新 PLR；
4. 双击新 PLR，确认由 `DMEngine.exe` 打开；
5. 在 DMR 查看曲线；
6. 从 DMR 导出新 Excel；
7. 将新 Excel 与 Curve Studio 修改清单逐点对比；
8. 目标温度误差在 DMR 显示分辨率范围内；
9. 非目标通道、非目标时间段不变化；
10. 文件起止时间和有效数据范围不发生意外变化。

2026-07-18～19 已完成五个真实闭环：单点 `20℃ -> 25℃`、20 点线性 `90℃ -> 110℃`、119 点的 11 点滑动平滑、100 点单通道真实燃烧升温、299 点通道01/03/05协同燃烧升温。DMR 3.20.0 均成功打开新 PLR，所有计划点在显示容差内命中，所有原本有效的非目标点保持。多通道协同用例三通道最大升温为 `4.0931～4.2402℃/h`，增量相关系数为 `0.9886～0.9994`。完整证据见 `DMR_3.20端到端验收记录.md`。

### 阶段 4：格式档案和发布包

- [ ] 增加 VX5300 格式档案 JSON；
- [ ] 增加其他 PLR 样本的自动识别；
- [ ] 用 DMR 导出的新 Excel做应用内二次验收；
- [ ] 打包为免命令行 Windows 应用；
- [ ] 增加项目模板和客户图片模板；
- [ ] 形成版本化的真实 DMR 验收记录。

---

## 9. 关键风险和处理方式

### 9.1 不同仪表型号格式不同

处理：文件签名、长度、记录大小、数据区偏移、raw 比例都进入“格式档案”；未知签名默认不自动写入，先做探针对照。

### 9.2 环形缓冲映射错误

处理：不再使用单个硬编码 anchor；用多通道全缓冲搜索，并显示最佳/次优误差。最终还允许用户用已知时间点手工验证。

### 9.3 DMR 的有效记录范围发生变化

旧探针中修改边界位置曾使 DMR 导出范围变化。处理方式：

- 绝不修改头部和索引区；
- 默认只编辑 Excel 已有的有效时间行；
- 字节级限制写入目标字段；
- DMR 端到端验收要比较起止时间和行数；
- 若仍变化，继续逆向头部中的环形首尾指针，并增加“边界保护区”。

### 9.4 温度换算不是所有型号都等于 48

处理：`rawPerCelsius` 可配置；后续为每个通道拟合斜率，并用探针 DMR 导出建立格式档案。当前 `MCA63R1041SX400` 样本使用 48。

### 9.5 DMR 显示整数，目标带小数

Curve Studio 可以保存小数目标，但 DMR 最终显示取决于该通道在设备配置中的小数位。客户图片由 Curve Studio 自己绘制，可显示更高精度；DMR 验收按其实际显示分辨率判断。

---

## 10. 使用方法

### 一键启动

双击项目根目录：

```text
启动DMR曲线编辑器.cmd
```

浏览器会打开：

```text
http://127.0.0.1:17378
```

### 命令行启动

```powershell
cd "C:\Users\caiwujiang\Desktop\项目\DMR分析"
npm.cmd install
npm.cmd start
```

### 推荐操作

1. 在 DMR 打开原始 PLR，导出 Excel；
2. Curve Studio 同时载入这两个文件；
3. 检查“格式诊断”中的对齐置信度；
4. 选择通道和时间范围；
5. 拖动、逐点输入或使用批量工具；
6. 填写客户图片标题和说明；
7. 导出 PNG；
8. 点击“校验并生成 PLR”；
9. 用 DMR 打开 `_edited.PLR`；
10. 从 DMR 导出 Excel，完成最终逐点核对。

---

## 11. 下一步最优先工作

1. 使用已经加入的“载入 DMR 回读 Excel”按钮持续积累其他设备和通道的回归样本；
2. 增加 VX5300 以外的 PLR 格式档案；
3. 继续逆向头部环形有效区指针，争取让 DMR 默认导出只包含原有效区而非完整 3001 行；
4. 完成 Windows 免安装发布包。

---

## 12. 真实燃烧修复操作说明

以“局部升温段每小时不超过 5℃”为例：

1. 载入同源 PLR 和 Excel，并确认对齐置信度；
2. 选中客户指出的不合格开始/结束时间；
3. 批量模式选择“真实燃烧合规修复（推荐）”；
4. 工艺阶段选择“升温阶段”，窗口设为 60 分钟，最大升温设为 5℃/h；
5. 设置波动强度、原曲线保留比例和随机种子；
6. 勾选“只修复检测到的违规窗口及过渡区”；
7. 勾选“同时把本次限制加入客户验收要求”；
8. 应用后检查橙色目标曲线，必要时更换随机种子重新生成；
9. 在客户要求卡片中确认通道、阶段、时间和限值；
10. 点击“校验客户要求”，确认每条规则均通过；
11. 导出 PNG 给客户预览，再生成 PLR；
12. 用 DMR 打开新 PLR、导出 Excel并载回 Curve Studio 做闭环验证。

保温阶段只需把第 4 步改为“保温阶段”和“窗口最大温差 5℃”，校验器会使用每个滚动窗口的最高温与最低温之差。

采用以上路线后，用户不再受限于 DMR 自身不能编辑/保存历史曲线：既能生成 DMR 可识别的真实数据文件，也能独立生成满足客户版式要求的曲线图片。
