# 断连补全、手动纵轴与停止脚本测试记录

- 测试日期：2026-07-20
- 测试对象：DMR Curve Studio 0.1.0
- 测试分支：`main`
- 测试基线：`35202ac`（本次变更提交前）

## 1. 测试范围

本轮回归覆盖以下新增或调整功能：

1. 热电偶断连点的批量补全：线性插值、最近有效值、指定温度和最长空白限制；
2. 断连点的表格逐点录入、清除，以及图表直接拖动补绘；
3. 补全值写回 PLR 时的绝对温度换算和字节级安全校验；
4. 图表手动纵轴：最低温度、最高温度、每格温度及恢复自动范围；
5. PNG 导出沿用手动纵轴；
6. 本地服务一键停止、运行记录清理和 Windows 便携包内容。

同时执行全部既有回归，确认环形缓冲区对齐、高温 `uint16`、真实燃烧修复、客户规则、多通道协同和 DMR 3.20.0 回读没有退化。

## 2. 测试环境

```text
操作系统：Microsoft Windows 11 专业版 10.0.26200 build 26200
架构：AMD64
Node.js：v24.15.0
npm：11.12.1
浏览器自动化：Playwright CLI / Chromium
服务地址：http://127.0.0.1:17378
时区：Asia/Shanghai
```

真实样本：

```text
fixtures/DAT0131.PLR
SHA256 596576CD37F239852FA084F289B2C328F4BAFB25C78D25825C50A3A3345A2744

fixtures/curve.xls
SHA256 B75B322743CBF64B75931AFD84AE9466B6C816C205A0739382E6E36F93F90C10
```

样本解析基准：

```text
时间点：2329
温度通道：9
Excel 首行物理记录：530
PLR 文件长度：76238 字节
数据区偏移：4214
记录结构：3001 × 24 字节
raw / ℃：48
```

## 3. 自动化测试结果

执行：

```powershell
npm.cmd run check
npm.cmd test
```

结果：

```text
语法检查：通过
tests：30
pass：30
fail：0
cancelled：0
skipped：0
todo：0
duration：4417 ms
```

### 3.1 回归矩阵

| 测试域 | 主要验证内容 | 结果 |
|---|---|---|
| PLR/Excel 解析 | 通道识别、无效温度、扩展通道边界、环形缓冲区自动对齐 | 通过 |
| 断连补全 | 线性、最近值、指定值、最长空白限制、首尾空白 | 通过 |
| PLR 写入安全 | 有效点增量写入、断连点绝对温度写入、头部和非目标字节保护 | 通过 |
| 高温兼容 | `uint16` 跨越 32767 边界时仍能安全写入 | 通过 |
| 批量曲线 | 偏移、线性、限幅、平滑、单位时间温差 | 通过 |
| 真实燃烧修复 | 固定种子复现、非固定周期、升温/保温/降温限制、局部修复 | 通过 |
| 多通道 | 共同波动、联合违规掩码、协同绘制、缺失点处理 | 通过 |
| 客户规则和项目 | 规则校验、项目 JSON 往返 | 通过 |
| DMR 回读 | DMR 3.20.0 单通道和多通道真实回读逐点比对 | 通过 |
| 本地服务 | 健康检查、核心脚本和示例文件 | 通过 |

## 4. 真实 DAT0131 断连补全

自动化测试先读取真实 PLR 与同源 Excel，再检查通道03。检测到的全部断连索引为：

```text
[0, 1, 2271, 2272, 2324, 2325, 2326, 2327, 2328]
```

使用“前后有效点直线插值”、`2 min` 采样间隔、最长空白 `0`（不限）后，实际补全结果为：

| 时间 | 目标温度 | 新 raw |
|---|---:|---:|
| 2023-11-06 10:00:00 | 19℃ | 912 |
| 2023-11-06 10:02:00 | 19℃ | 912 |
| 2023-11-09 13:42:00 | 79℃ | 3792 |
| 2023-11-09 13:44:00 | 79℃ | 3792 |
| 2023-11-09 15:28:00 | 24℃ | 1152 |
| 2023-11-09 15:30:00 | 24℃ | 1152 |
| 2023-11-09 15:32:00 | 24℃ | 1152 |
| 2023-11-09 15:34:00 | 24℃ | 1152 |
| 2023-11-09 15:36:00 | 24℃ | 1152 |

断连位置没有可信原 raw 可用于增量计算，因此写入规则为：

```text
rawNew = round(targetTemp × 48)
```

验证结果：

```text
断连点：9
生成编辑：9
gapFilled 标记：9/9
原有效温度变化：0
计划变化 16-bit 字：9
实际变化字节：18
文件长度：76238 -> 76238
PLR 头部变化：0
非目标字节变化：0
兼容性校验：通过
CSV 标记：gap_filled = yes
负温度绝对写入：拒绝
超过 uint16 上限的绝对写入：拒绝
局部选区绕过整段空白长度限制：拒绝
```

## 5. 浏览器 UI 回归

### 5.1 示例载入

点击“载入项目示例”后观察到：

```text
PLR：DAT0131.PLR · 74.5 KB
XLS：curve.xls · 364.0 KB
状态：解析成功，2329 个时间点、9 个温度通道
自动物理起点：530
对齐平均误差：0.821℃
对齐置信度：高
```

### 5.2 批量补全

操作：

1. 编辑通道切换为通道03；
2. 处理方式选择“补全断连空白”；
3. 补全方式保持“前后有效点直线插值”；
4. 最长补全空白保持 `0`；
5. 应用到通道03全部 2329 个点。

观察结果：

```text
修改计数：0 -> 9
首行原始值：断连
首行目标值：19
首行状态：已补全
第二行原始值：断连
第二行目标值：19
第二行状态：已补全
撤销后修改计数：9 -> 0
重做按钮：可用
```

### 5.3 图表断点补绘

操作：

1. 保持“断点补绘：开”和“绘制”模式；
2. 将选区设置为索引 `0～30` 并点击“图表聚焦范围”；
3. 从曲线开头拖动，穿过通道03的两个断连点。

观察结果：

```text
修改计数：0 -> 4
索引 0：断连 -> 53.9215℃，已补全
索引 1：断连 -> 60.5882℃，已补全
索引 2：19.00℃ -> 67.2549℃
索引 3：20.00℃ -> 68.9215℃
撤销后修改计数：4 -> 0
```

该结果证明一次连续手势可同时覆盖空白点和已有有效点，并作为一条撤销记录恢复。

### 5.4 手动纵轴

输入并应用：

```text
最低：0℃
最高：500℃
每格：25℃
```

界面状态：

```text
固定 0 ～ 500 ℃，每格 25 ℃
图表状态：Y轴 0～500℃ / 25℃
```

导出 PNG 后进行原始分辨率目视检查：

```text
建议文件名：DAT0131_curve.png
测试产物：output/playwright/manual-axis-0-500-step-25.png
图像：1800 × 1000，RGBA
SHA256：815A212E6D8620E551920738B437325EEBFD10C2617477DA40F61E5A0B811F44
纵轴首尾：0℃ / 500℃
刻度间隔：25℃
曲线和图例：显示正常，无裁切
```

### 5.5 控制台

两次浏览器流程结束时分别读取错误和警告：

```text
Errors：0
Warnings：0
```

## 6. 启动与停止闭环

执行：

```powershell
npm.cmd run stop
$env:DMR_NO_BROWSER = '1'
npm.cmd run launch
npm.cmd run stop
```

实际结果：

```text
START_HEALTH=True
APP=DMR Curve Studio
VERSION=0.1.0
START_PID=35036
STOP_HEALTH=True（停止后连接失败，符合预期）
PID_FILE_EXISTS=False
INFO_FILE_EXISTS=False
```

结论：启动器在健康检查通过后返回；停止器能够找到监听端口对应的服务进程并停止；停止后 `.runtime/dmr-server.pid` 与 `.runtime/dmr-server.json` 均已清理。`停止DMR曲线编辑器.cmd` 与 `npm.cmd run stop` 使用同一停止实现。

## 7. Windows 便携包

执行：

```powershell
npm.cmd run package:win
```

产物：

```text
dist/DMR-Curve-Studio-v0.1.0-win-x64.zip
大小：34717482 字节
SHA256：F65060F566EF9D20621176D988A442F2994167023DB0CD901C9283F487676BD2
```

ZIP 内逐项确认存在：

```text
启动DMR曲线编辑器.cmd
停止DMR曲线编辑器.cmd
scripts/start_dmr.js
scripts/stop_dmr.js
public/app.js
```

## 8. 已知边界

1. 本轮新增的“断连点绝对温度写入”没有另行启动 DMR GUI 做一次只针对断连点的手工 Excel 导出闭环；
2. 已保存的 DMR 3.20.0 单点、批量、真实燃烧和多通道回读回归仍全部通过；
3. 新断连写入已直接验证公式、目标字段、文件长度、头部和所有非目标字节，因此不会把无效哨兵值误当成普通温度增量；
4. 便携 ZIP 包含构建时间信息，重新构建后即使内容等价，ZIP SHA-256 也可能变化，应以同次构建生成的校验值为准；
5. PNG 测试产物位于已忽略的 `output/playwright`，用于本机目视验证，不纳入发布包。

## 9. 结论

本轮所有语法检查、30 个自动化测试、真实 DAT0131 字节安全检查、浏览器交互、PNG 纵轴、启动/停止闭环和便携包内容检查均通过。

新增功能满足以下交付条件：

```text
断连空白可批量或直接绘图补全
有效温度不被批量补全误改
断连值按绝对 raw 安全写入
手动纵轴同时作用于界面和 PNG
服务可一键停止并清理运行记录
便携包同时包含启动与停止入口
```
